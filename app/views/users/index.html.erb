<% provide(:title, 'ユーザー一覧') %>
<% provide(:user, "#{current_user.name}さん") %>
<% @users.each do |user| %>
	<div>
		<h2 class="contents">
			<%= link_to user.name, user_path(user) %>
		</h2>
	</div>
<% end %>